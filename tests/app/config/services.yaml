imports:
  - { resource: ../../../config/services.yaml }

services:
  _defaults:
    autowire: true
    autoconfigure: true
    public: true

  Mike4Git\ChainBundle\Tests\Handler\SampleHandler:
    tags:
      - { name: 'chain.handler', chain: 'example', priority: 100 }
    public: true
